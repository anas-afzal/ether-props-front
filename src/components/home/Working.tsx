import React from 'react';

const Working : React.FC = () => {
    return (
        <div id = "working" className = "flex items-center justify-center h-screen bg-gray-200">
            <h1 className = "text-3xl text-gray-700 font-light">How it works</h1>
        </div>
    );
};

export default Working;